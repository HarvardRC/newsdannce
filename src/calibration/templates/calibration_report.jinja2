<!DOCTYPE html>
<html>
<head>
    <title>Calibration Report</title>
    <style>
    h1, h2 {
        line-height: 15px;
    }
    </style>
</head>
<body>
    <h1>Calibration Report</h1>
    <h2>Logs</h2>
    <pre>
    {{logs_text}}
    </pre>
    <h2>Intrinsincs Per-Image Re-Projection Errors (RPE)</h2>
    <div>
        {% for imagepath in rpe_imagepaths %}
            <img src="{{imagepath}}"> </img>
        {% endfor %}
    </div>
    <h2>Extrinsincs Re-projected Points</h2>
    <div>Note: rainbow marks are detected image points (corners) and red marks are re-projected points</div>
    <div style="display:flex; width:100%">
        {% for imagepath in ext_reproj_imagepaths %}
            <img src="{{imagepath}}" style="width: 100%;"> </img>
        {% endfor %}
    </div>
</body>
</html>